{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "generate-code",
            "type": "shell",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -File \"${workspaceFolder}/.vscode/codegen-if-needed.ps1\"",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "focus": false
            },
            "problemMatcher": []
        },
        {
            "label": "generate-code (force)",
            "type": "shell",
            "command": "flutter_rust_bridge_codegen generate; dart run build_runner build",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "focus": false
            },
            "problemMatcher": []
        },
        {
            "label": "Build Release",
            "type": "shell",
            "command": "flutter build ${input:platform} --split-debug-info=debug_info",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "dependsOn": [
                "generate-code"
            ],
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "platform",
            "type": "pickString",
            "description": "Choose platform for release",
            "options": [
                "windows",
                "linux",
                "macos"
            ],
            "default": "windows"
        }
    ]
}
