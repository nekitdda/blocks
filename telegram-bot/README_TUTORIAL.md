# Запуск
cd telegram-bot
pip install -r requirements.txt
python bot.py

# Разделы
Файл telegram-bot/config.py:23
Меняй только текст справа. Ключи слева не трогай.

# Лого
Файл telegram-bot/assets/logo.png
Замени своим файлом с тем же именем. Сейчас там темная заглушка 800x800.
Если бот стартует без фото - смотри лог: "logo not found" или "logo send failed".
Путь проверяется через config.LOGO_PATH.

# Админ
/myid -> число в .env в ADMIN_IDS -> перезапуск -> /admin

# Позиции
/admin -> Добавить позицию -> раздел -> название -> описание -> фото/файл
Пользователь: жмет раздел -> видит список -> жмет кнопку или пишет название -> бот отправляет.
